// app/ui/fonts.ts
import { Inter } from 'next/font/google';
import { Lusitana } from 'next/font/google';

// Export the fonts with required properties
export const inter = Inter({ subsets: ['latin'], weight: ['400', '700'] }); // You can adjust weights as needed
export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'] }); // Adjust weights as needed


